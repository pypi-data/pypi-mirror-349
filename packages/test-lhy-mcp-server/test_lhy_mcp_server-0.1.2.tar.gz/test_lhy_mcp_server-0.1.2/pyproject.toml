[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-lhy-mcp-server"
version = "0.1.2"
description = "test"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Rongcloud", email = "imsdk@rongcloud.cn" }
]
dependencies = [
    "mcp[cli]",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "click>=8.0.0"
]

[project.optional-dependencies]
dev = [
    "pip-tools"
]

[project.scripts]
test-lhy-mcp-server = "src.server.server:main"

[project.urls]
Homepage = "https://github.com/rongcloud/test-lhy-mcp-server"
Repository = "https://github.com/rongcloud/test-lhy-mcp-server.git"

[tool.hatch.build.targets.wheel]
packages = ["src", "lib"]
include = [
    "src/**/*.py",
    "lib/**/*.py",
    "lib/**/*.so",
    "lib/**/*.dylib",
    "lib/**/*.dll"
]

[tool.hatch.build]
exclude = [
    "src/imsdk/rust_db/",
    "src/imsdk/rust_db/**/*",
    "lib/rust_db/",
    "lib/rust_db/**/*",
    "**/*.db",
    "**/*.db-shm",
    "**/*.db-wal",
    "**/*.kv",
    ".vscode/",
    ".idea/",
    ".env",
    ".venv*",
    ".DS_Store",
    ".gitignore",
    ".cursorrules",
    "config.json",
    "setup.py",
    "main.py",
    ".python-version"
]
artifacts = [
    "src/imsdk/rust_db/**",
    "lib/rust_db/**"
]

[tool.hatch.metadata]
allow-direct-references = true

