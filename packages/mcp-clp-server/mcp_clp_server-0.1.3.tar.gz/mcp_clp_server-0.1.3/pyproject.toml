[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-clp-server"
version = "0.1.3"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.3.4",
    "psutil>=7.0.0",
]

[project.scripts]
mcp-clp-server = "mcp_clp_server.__main__:main"