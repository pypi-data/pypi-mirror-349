[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mal-test-package"
version = "0.0.1"
readme = "README.md"

dependencies = [
    "build>=1.2.2.post1",
    "setuptools>=80.8.0",
    "twine>=6.1.0",
    "wheel>=0.45.1",
]
