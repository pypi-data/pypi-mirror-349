[project]
name = "mseep-mcp-server-gemini-image-analyzer"
version = "0.1.1"
description = ""
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]
keywords = [
    "http",
    "mcp",
    "llm",
    "automation",
]
dependencies = [
    "fastmcp>=0.4.1",
    "google>=3.0.0",
    "google-genai>=1.7.0",
    "pillow>=11.1.0",
]
maintainers = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.license]
text = "MIT"

[tool.hatch.build.targets.wheel]
packages = [
    "server",
]

[tool.uv]
dev-dependencies = [
    "pyright>=1.1.389",
    "ruff>=0.7.3",
]

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"
