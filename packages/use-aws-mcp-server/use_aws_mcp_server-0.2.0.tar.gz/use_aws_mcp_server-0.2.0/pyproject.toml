[project]
name = "use-aws-mcp-server"
version = "0.2.0"
description = "MCP server that provides AWS CLI integration for AI assistants, enabling execution of AWS API calls with proper parameter validation and error handling"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "moritalous"},
]
dependencies = [
    "strands-agents-tools>=0.1.1",
]

[project.scripts]
use-aws-mcp-server = "use_aws:main"
