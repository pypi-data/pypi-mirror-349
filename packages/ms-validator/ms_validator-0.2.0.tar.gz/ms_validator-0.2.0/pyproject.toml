[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ms_validator"
version = "0.2.0"
description = "A package for validating ."
authors = [
    { name = "Jiwang Liu" }
]
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["ms_validator"]

[tool.setuptools.package-data]
"*" = ["*.json"]
