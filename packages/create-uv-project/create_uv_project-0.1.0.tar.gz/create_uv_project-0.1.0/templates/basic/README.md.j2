# {{ project_name }}

This is a basic Python project generated by `create-uv-project`, following a modern src-layout structure.

## Project Structure

```
{{ project_slug }}/
├── src/
│   └── {{ project_slug }}/
│       ├── __init__.py
│       └── main.py
├── tests/
│   ├── __init__.py
│   └── test_main.py
├── config/
│   └── settings.yaml
├── pyproject.toml
├── README.md
└── .gitignore
```

## Getting Started

1.  Navigate to the project directory:
    ```bash
    cd {{ project_slug }}
    ```
2.  Set up the virtual environment and install dependencies using UV:
    ```bash
    uv venv
    source .venv/bin/activate  # On Windows: .venv\Scripts\activate
    uv pip install -e ".[dev,test]"
    ```
3.  Run the main application:
    ```bash
    {{ project_slug }}
    # Or directly run the main module:
    python -m {{ project_slug }}.main
    ```
4.  Run tests:
    ```bash
    uv run pytest
    ```

## Configuration
Project-specific configurations can be found in the `config/` directory (e.g., `settings.yaml`).

## Development
- Linting: `uv run ruff check .`
- Formatting: `uv run black .` 