# src/{{ project_slug }}/commands/__init__.py

# This file makes Python treat the directory `commands` as a package.
# You can optionally import and re-export command modules or apps here if needed.

from . import example # Make the example command module available 