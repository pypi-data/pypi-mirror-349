Environment variables for {{ project_name }}
# This is an example file. Copy it to .env in your generated project and fill in your actual values.

# --- Application Settings (see src/{{ project_slug }}/core/config.py) ---
APP_NAME="{{ project_name }}"
DEBUG_MODE=true
# API_V1_STR="/api/v1" # If you use API versioning prefix

# --- Database Settings (Example - Uncomment and configure if using a database) ---
# Choose one of the following or add your own:

# PostgreSQL (requires asyncpg driver for FastAPI/SQLModel/Tortoise ORM with async)
# DATABASE_URL="postgresql+asyncpg://user:password@host:port/dbname"

# MySQL (requires aiomysql or similar async driver)
# DATABASE_URL="mysql+aiomysql://user:password@host:port/dbname"

# SQLite (relative path, creates a file in the project root or instance folder)
# DATABASE_URL="sqlite+aiosqlite:///./{{ project_slug }}.db"
# Or for an in-memory SQLite database (useful for testing, data is lost on app shutdown):
# DATABASE_URL="sqlite+aiosqlite:///:memory:"

# SQLALCHEMY_ECHO=false # Set to true for SQLAlchemy to log SQL queries (useful for debugging)

# --- Security Settings (Important!) ---
# SECRET_KEY="<generate_a_strong_random_secret_key_here_for_jwt_or_other_security_features>"
# ALGORITHM="HS256" # Algorithm for JWT, if used
# ACCESS_TOKEN_EXPIRE_MINUTES=30 # Example for JWT

# --- Uvicorn Server Settings (often overridden by CLI or process manager in deployment) ---
# These are defaults if not specified when running `uvicorn` directly without the `dev` script,
# or if your `run_dev` script in `main.py` doesn't specify them.
# HOST="127.0.0.1"
# PORT=8000
# WORKERS=1 # For production, consider a value like $(($(nproc) * 2 + 1))
# RELOAD=false # Set to true for development if not using the `dev` script's reload
# LOG_LEVEL="info"

# --- CORS (Cross-Origin Resource Sharing) Settings (Example) ---
# If your frontend is on a different domain/port, you might need to configure CORS.
# BACKEND_CORS_ORIGINS='["http://localhost:3000", "http://127.0.0.1:3000", "https://your-frontend-domain.com"]' # JSON-formatted string list

# --- Other External Services or Custom Settings ---
# EXTERNAL_API_KEY_SERVICE_X="your_api_key_for_service_x"
# CUSTOM_SETTING_FOO="bar"