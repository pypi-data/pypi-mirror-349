[project]
name = "frprint"
version = "1.2.3"
description = "more easier to use for-print"
authors = [
    {name = "howShouldIChosseMyUsername", email = "trademark2179@gmail.com"}
]
readme = "Readme.md"
license = { text = "MIT" }
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["frprint"]