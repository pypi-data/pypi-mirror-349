[project]
name = ""
version = "0.0.1"
description = ""
requires-python = ">=3.9,<3.13"

dependencies = [
]

# This is the entry point for the dlt+ project package
[project.entry-points.dlt_package]
dlt-project = ""

# section for adding dlt.yml file to the project
[tool.setuptools.package-data]
