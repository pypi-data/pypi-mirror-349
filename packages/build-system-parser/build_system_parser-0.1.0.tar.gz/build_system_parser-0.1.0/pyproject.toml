[project]
name = "build_system_parser"
version = "0.1.0"
description = "Control all Build Systems"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

authors = [
  { name = "FloydZ", email = "floyd@example.com" }
]

maintainers = [
  { name = "FloydZ", email = "floyd@example.com" }
]

dependencies = [
    "pylint",
    "pytest",
]

[project.urls]
"Homepage" = "https://github.com/FloydZ/python_builder"
"Repository" = "https://github.com/FloydZ/python_builder"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
