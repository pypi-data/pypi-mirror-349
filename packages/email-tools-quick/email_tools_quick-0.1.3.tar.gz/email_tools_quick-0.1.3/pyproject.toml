[project]
name = "email-tools-quick"
version = "0.1.3"
description = "A collection of tools for email processing"
requires-python = ">=3.10"
readme = "README.md"
dependencies = [
    "beautifulsoup4",
    "curl-cffi",
    "loguru",
    "pysocks",
    "python-dateutil",
]

[dependency-groups]
dev = [
    "pysocks>=1.7.1",
    "python-dotenv>=1.1.0",
]
