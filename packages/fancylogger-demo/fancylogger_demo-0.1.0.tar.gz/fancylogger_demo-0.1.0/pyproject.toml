[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fancylogger_demo"
version = "0.1.0"
description = "Just a demo"
requires-python = ">=3.12"
authors = [{ name="Kevin", email="foo@example.com" }]
dependencies = [
    "pandas>=2.2.3",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "twine>=6.1.0",
]

