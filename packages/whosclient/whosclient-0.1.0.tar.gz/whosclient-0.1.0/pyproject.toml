[project]
name = "whosclient"
version = "0.1.0"
dependencies = ["click", "requests", "pandas", "plotly", "nbformat"]

[tool.setuptools]
packages = ["whosclient"]
package-dir = {"" = "src"}

[project.scripts]
whosclient = "whosclient.__main__:cli"
