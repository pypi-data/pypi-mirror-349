[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zModule"
version = "0.0.2"
description = "Nothing here for the moment."
authors = [
    { name = "zisia13", email = "your.email@example.com" }
]
license = { text = "MIT" }
dependencies = []