{{ entity._module.name|upper }}_{{ entity.name|upper }}S = [
    {
        {% for field_details in entity.fields.values() %}
        {% if field_details.example == "CHANGEME" %}
        # CHANGEME: Insert valid data
        {% endif %}
        "{{ field_details.name }}": "{{ field_details.example }}",
        {% endfor %}
    }
]
