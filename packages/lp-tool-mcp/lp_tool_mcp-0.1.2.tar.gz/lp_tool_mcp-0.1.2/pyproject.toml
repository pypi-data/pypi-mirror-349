[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "lp-tool-mcp"
version = "0.1.2"
description = "lp tool mcp"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.3.5",
    "psutil>=7.0.0",
]




[project.scripts]
lp-tool-mcp = "lp_tool_mcp:main"