[project]
name = "addition-numpy-pytorch-package"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yerly Sevan", email = "sevan.yerly@he-arc.ch" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.6",
]

[project.scripts]
addition-numpy-pytorch-package = "addition_numpy_pytorch_package:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
