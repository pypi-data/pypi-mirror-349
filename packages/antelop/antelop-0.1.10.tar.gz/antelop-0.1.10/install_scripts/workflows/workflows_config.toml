[paths]
install_dir = "/public/singularity/containers/antelop" # Install path
data_dir = "/cephfs/${USER}/.antelop"                  # Data path
work_dir = "/net/nfs1/netscr/${USER}/antelop"          # Scratch space

[misc]
email = "${USER}@mrc-lmb.cam.ac.uk" # Email address to send notifications to when jobs run
unix_group = "trilab"

[install] # Which of the three components to install (true/false)
workflows = true
containers = false
nextflow = false
