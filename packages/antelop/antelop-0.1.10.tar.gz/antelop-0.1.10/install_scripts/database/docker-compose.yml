version: "2.2"
services:
  mysql:
    container_name: mysql
    image: datajoint/mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=<enter-root-password-here>
    ports:
      - "3306:3306"
    volumes:
      - mysql:/var/lib/mysql
    restart: always
  s3:
    container_name: s3
    image: minio/minio:latest
    volumes:
      - s3:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=root
      - MINIO_ROOT_PASSWORD=<enter-root-password-here>
      - MINIO_API_CORS_ALLOW_ORIGIN=*
    command: server --console-address ":9001" /data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: always

volumes:
  mysql:
  s3:
