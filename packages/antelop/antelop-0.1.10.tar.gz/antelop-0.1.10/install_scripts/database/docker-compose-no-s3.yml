version: "2.2"
services:
  mysql:
    container_name: mysql
    image: datajoint/mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=<enter-root-password-here>
    ports:
      - "3306:3306"
    volumes:
      - mysql:/var/lib/mysql
    restart: always

volumes:
  mysql:
