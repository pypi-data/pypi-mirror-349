[build-system]
requires = ["setuptools", "wheel", "pillow"]
build-backend = "setuptools.build_meta"

[project]
name = "easyimage"
version = "0.0.1"
description = "A lot of cool image editing functions that can be used easily (based on PIL)"
readme = "README.md"
license = {text = "GPL-3.0"}
authors = [
  { name="Anvarys", email="anvarys.anon@gmail.com" }
]
dependencies = []

[project.urls]
Homepage = "https://github.com/anvarys/easyimage"

[tool.setuptools.packages.find]
where = ["."]
