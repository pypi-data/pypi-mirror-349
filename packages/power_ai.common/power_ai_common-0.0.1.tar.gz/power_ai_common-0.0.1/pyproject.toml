[tool.poetry]
name = "power_ai.common"
version = "0.0.1"
description = "公共基础模块"
requires-python = ">=3.7"
authors = ["Guanghui Zhang"]
packages = [
    {include = "power_ai"},
]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
