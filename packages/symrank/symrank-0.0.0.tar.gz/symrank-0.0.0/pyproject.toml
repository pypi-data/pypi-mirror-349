[project]
name = "symrank"
version = "0.0.0"
description = "Placeholder to use the name for first time."

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"