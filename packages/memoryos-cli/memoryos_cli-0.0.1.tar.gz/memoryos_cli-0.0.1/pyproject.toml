[project]
name = "memoryos-cli"
version = "0.0.1"
description = "A standalone command-line interface tool for interacting with MemoryOS."
authors = [
    {name = "dany",email = "973031439@qq.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "memoryos_cli", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
